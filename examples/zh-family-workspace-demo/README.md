# Family OS 家庭工作区

这是一个私有家庭工作区。家庭资料只保存在这里，不写入 Family OS 技能包。

## 使用流程

1. 原始资料先放入 `00-inbox/`。
2. 当前重点维护在 `01-dashboard/`。
3. 长期领域沉淀在 `02-areas/`。
4. 阶段性事务推进在 `03-projects/`。
5. 重大决策记录在 `04-decisions/`。
6. 稳定事实和偏好保存在 `05-memory/`。
7. 周期复盘放入 `90-reviews/`。
8. 过期资料归档到 `99-archive/`。
